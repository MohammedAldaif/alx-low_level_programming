#include<stdio.h>
#include<string.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char str[] = "and";
	for(int i=0;i<2;i++)
	{
		putchar(str[i]);
	}
	putchar('\n');
	return (0);
}
